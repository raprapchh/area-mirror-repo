name: Simple CI Validation Area Mirror

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate files
        run: |
          echo "✅ CI is running!"
          echo "Here are the files in the repo:"
          ls -R
